rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read/write access to all users under any path in your Storage bucket
      allow read, write: if request.auth != null;
    }
  }
}

